import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('placeholder', (WidgetTester tester) async {
    // Wallet tests require a running ShardCoin node
    expect(true, isTrue);
  });
}
